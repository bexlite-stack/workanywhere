version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - type: volume
        source: public_data
        target: /app/public

volumes:
  public_data:
    name: public_data
